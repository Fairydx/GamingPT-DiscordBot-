exports.run = (client) => {
    const config = require('./config.json');
    client.user.setGame(config.prefix + "ajuda")
    console.log(`Em funcionamento!!!! total de ${client.channels.size} Canais, em ${client.guilds.size} servers, um total de ${client.users.size} utilizadores.`)
}